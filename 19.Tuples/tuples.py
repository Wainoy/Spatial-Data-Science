#tuples
#do not support append() functions or any form of ammendments
#they only support count() and index(). they are useful in creating a list of items where we'd like to avoid them being edited anywhere in the code
numbers = (1,2,3,4,4,5)
print(numbers[0])
numbers.count(4)

