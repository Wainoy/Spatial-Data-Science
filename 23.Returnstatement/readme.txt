Return statement is especially useful in numerical calculations
by default all functions in python return null (None)
you can change that by adding the return statement to the result of a function to return a value.