pathlib library from python 3 module index
Path class from pathlib module
there are absolute and relative paths
in absolute paths, we start from the root directory, e.g C:\Users\local
in relative paths we start from the current directory.

Python package inde (PyPi)
- used to install packages into python to perform more complex, more diverse functions
- e.g functions to scrap the web, functions to send text messages etc
- are installed from the terminal e.g.
	pip install openpyxl #a package for manipulating excel files


