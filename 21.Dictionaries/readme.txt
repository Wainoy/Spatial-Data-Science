dictionaries.py contains examples about dictionaries
dictionaries are used to store values that occur as key value pairs.
They are defined using {}
each key is unique. it is possible to change the value items. 
It also possible to append new items onto the dictionary
items can be accessed using [] and the key.
dictionaries can support multiple data types